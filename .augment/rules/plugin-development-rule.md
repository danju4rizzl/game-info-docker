---
type: 'manual'
---

Build a plugin we can so we can reuse for any game form pandascore. That expose a short code, when added to the site it will display the scores. Users should be able to change the settings from a UI
